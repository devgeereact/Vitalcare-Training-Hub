// "My sessions" and "Sessions" are now a single role-aware page. This route
// stays alive for any existing links and renders the unified page: learners see
// their booked sessions, staff see the full schedule.
export { default } from "./SessionsListPage"
